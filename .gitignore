# Binaries
release-notifier
release-notifier.exe

# Config with real credentials — never commit these
config.yaml
*.csv

# Keep the examples
!config.yaml.example
!recipients.csv.example
!release-notes.md.example

# Go build cache
*.test
*.out
*.exe
/vendor/

# Temp PowerShell scripts generated at runtime
release-notifier-*.ps1

# godotenv file
*.env
